kind = "web"
previewPath = "/school-management"
title = "EduManager — School Management"
version = "1.0.0"
id = "EduManager_SchoolMgmt_v1"

[[services]]
localPort = 8000
name = "EduManager"
paths = ["/school-management"]

[services.development]
run = "cd artifacts/school-management && bash run.sh"
